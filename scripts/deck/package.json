{
  "name": "pratyaya-deck",
  "version": "1.0.0",
  "private": true,
  "description": "Generates the submission deck from the project's measured results.",
  "scripts": {
    "build": "node build_deck.js"
  },
  "dependencies": {
    "pptxgenjs": "^4.0.1"
  }
}
